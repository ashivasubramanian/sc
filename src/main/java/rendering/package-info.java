/**
 * The <code>rendering</code> package is responsible for obtaining current game data from the
 * <code>game_engine</code> layer, and updating the <code>presentation</code> layer with the current data. <br>
 */
package rendering;